export type TaskListType = {
  id: string;
  name: string;
  description: string;
  priority: string;
  due_date: string;
};
export type ColumnType = {
  name: string;
  key: string;
  type?: string;
};
